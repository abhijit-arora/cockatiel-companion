rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Default rule: Require authentication for all read/write operations.
    // We will add more specific rules (e.g., for profile pictures) later.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}